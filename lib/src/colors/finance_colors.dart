import 'package:flutter/material.dart';

class FinanceColors {
  // Primary Palette
  static const Color primary = Color(0xFF1E88E5);
  static const Color primaryLight = Color(0xFF6AB7FF);
  static const Color primaryDark = Color(0xFF005CB2);

  // Background
  static const Color background = Color(0xFFF5F7FA);
  static const Color surface = Colors.white;

  // Status Colors (e.g. goals, budgets)
  static const Color success = Color(0xFF4CAF50); // Verde poco gastado
  static const Color warning = Color(0xFFFF9800); 
  static const Color error = Color(0xFFF44336);   // Rojo muy gastado

  // Text Colors
  static const Color textPrimary = Color(0xFF2C3E50);
  static const Color textSecondary = Color(0xFF7F8C8D);
}
